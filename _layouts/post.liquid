<!DOCTYPE html>
<html lang="en" class="no-js">
{% include head %}
{% if site.banner.text %}
  <body class="banner-displayed">
{% else %}
  <body>
{% endif %}
  {% include menu %}
  <div class="container">
    <div class="row">
      <div class="col-md-8">
        <main class="blog">
          <article>
            {{ content }}
            <footer>
              <p class="text-center">ESLint依赖于个人和公司的支持。<a class="btn btn-default" href="https://opencollective.com/eslint">成为赞助商。</a></p>
            </footer>
          </article>
        </main>
      </div>
      <div class="col-md-4 hidden-xs hidden-sm">
        <div class="eslint-ad-float-left">
        {%include ad %}
        </div>
        <div>
          <h2>Recent Posts</h2>
          <ul class="posts">
            {% for post in collections.posts limit: 25 %}
              {% if post.url != page.url %}
                <li>
                  <a href="{{ post.url | replace: '.html', '' }}">{{ post.data.title }}</a><br>
                  <span class="date">{{ post.date | dateFormat: "d LLLL yyyy" }}</span>
                </li>
              {% endif %}
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
  {% include footer %}
</body>
</html>
